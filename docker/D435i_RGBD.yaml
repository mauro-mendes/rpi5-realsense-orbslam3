%YAML:1.0

# ORB-SLAM3 settings — Intel RealSense D435i, 640x480 RGB-D
# Nominal intrinsics. For better accuracy, replace fx/fy/cx/cy with values
# from: python -c "import pyrealsense2 as rs; ..."

Camera.type: "PinHole"

Camera.fx: 615.0
Camera.fy: 615.0
Camera.cx: 320.0
Camera.cy: 240.0

Camera.k1: 0.0
Camera.k2: 0.0
Camera.p1: 0.0
Camera.p2: 0.0

Camera.width: 640
Camera.height: 480
Camera.fps: 30

# 0=BGR input, 1=RGB input (producer sends RGB)
Camera.RGB: 1

# Not used for RGBD but required by parser
Camera.bf: 0.0

# Points further than ThDepth * baseline are considered "far" (ignored)
ThDepth: 40.0

# depth_meters = raw_uint16 / DepthMapFactor
# D435i depth_scale = 0.001 m/unit → DepthMapFactor = 1000
DepthMapFactor: 1000.0

ORBextractor.nFeatures: 1000
ORBextractor.scaleFactor: 1.2
ORBextractor.nLevels: 8
ORBextractor.iniThFAST: 20
ORBextractor.minThFAST: 7

# Viewer is disabled (false) when running headless in Docker
Viewer.KeyFrameSize: 0.05
Viewer.KeyFrameLineWidth: 1
Viewer.GraphLineWidth: 0.9
Viewer.PointSize: 2
Viewer.CameraSize: 0.08
Viewer.CameraLineWidth: 3
Viewer.ViewpointX: 0
Viewer.ViewpointY: -0.7
Viewer.ViewpointZ: -1.8
Viewer.ViewpointF: 500
